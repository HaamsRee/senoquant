# Settings

The Settings tab currently exposes one option:

- **Preload segmentation models on startup**: when enabled, the
  Segmentation tab will instantiate all discovered segmentation models
  as soon as the tab loads. This reduces first-run latency at the cost of
  additional startup time.
